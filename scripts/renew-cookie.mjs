/**
 * renew-cookie.mjs — DESATIVADO
 *
 * Todas as buscas ao SuperMonitor agora são feitas exclusivamente
 * pela extensão no navegador (sem risco de ban).
 *
 * Este script não faz nenhuma requisição ao SuperMonitor.
 */

console.log('[SureEdge] renew-cookie: desativado — extensão gerencia tudo.');
process.exit(0);
